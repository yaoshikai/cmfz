<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.ysk.mapper.BannerMapper">

    <select id="queryAllBanner" resultType="Banner">
        select banner_id "id",title,img_path imgPath,status,pub_date pubDate,description
        from banner limit
             ${(page-1)*rows},
             ${rows}
    </select>

</mapper>
